import { css } from "emotion";

const GlobalStyles = {
    FLAT_LINK: css`
        &:link {
            color: black;
            text-decoration:none;
        }

        &:visited {
            color: black;
            text-decoration:none;
        }

        &:hover {
            color: black;
            text-decoration:none;
        }

        &:active {
            color: black;
            text-decoration:none;
        }
    `
}

export default GlobalStyles;